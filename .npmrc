# The registry component source lives at the repo root (registry/), outside any
# workspace package, but imports npm deps like `radix-ui`. Hoist dependencies to
# the root node_modules so those files resolve their imports when the docs app
# transpiles them.
node-linker=hoisted
